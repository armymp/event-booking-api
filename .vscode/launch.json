{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Dev API",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go",
            "env": {
                "APP_ENV": "development",
                "DB_NAME": "dev.db"
            }
        },
        {
            "name": "Run Prod API",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go",
            "env": {
                "APP_ENV": "production",
                "DB_NAME": "events.db"
            }
        }
    ]
}